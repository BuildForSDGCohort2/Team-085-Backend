#!/bin/sh

# Flask App config
export FLASK_APP=app
export FLASK_ENV=development

# Database URI
# You're allowed to use other SQL databases besides PostgreSQL.
# Uncomment the next line and replace the username, password, port and database name.
# export DATABASE_PATH=postgres://<username>:<password>@<localhost:5432>/<database_name>
